{
  "name": "helpdesk",
  "private": true,
  "authorPersona": "Morgan Lee",
  "issue": "#42",
  "workspaces": [
    "packages/client",
    "packages/server",
    "packages/shared"
  ],
  "scripts": {
    "lint": "eslint \"packages/**/*.{ts,tsx}\" --no-error-on-unmatched-pattern",
    "format": "prettier --write \"**/*.{js,ts,tsx,json,md}\"",
    "test": "npm run test --workspace @helpdesk/server",
    "typecheck": "npm run build --workspace @helpdesk/shared && npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.1",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
